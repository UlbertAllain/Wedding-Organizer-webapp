rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Browser clients never access business collections directly.
    // All reads/writes pass through authenticated Next.js route handlers.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
